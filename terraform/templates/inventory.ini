###############################################
# This file is managed by Terraform. Do not edit.
###############################################

%{ for group_name, hosts in groups }
[${group_name}]
%{ for host in hosts }
${host.name}
%{ endfor }
%{ endfor }

[all:vars]
ansible_port = 22
ansible_user = ${username}
