## Slurm
slurm_nodes:
%{ for group_name, node in nodes ~}
%{ if group_name == "slurmexecservers" || group_name == "galaxyservers" ~}
%{ for host in hosts ~}
  - name: ${host.name}
    CPUs: ${host.cpu}
%{ endfor ~}
%{ endif ~}
%{ endfor ~}
