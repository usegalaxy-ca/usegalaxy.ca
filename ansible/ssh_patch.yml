- hosts: all
  become: true
  become_user: root
  tasks:
    - name: update ssh
      apt:
        name:
          - openssh-server
          - openssh-client
        update_cache: yes
        only_upgrade: yes
        state: latest
    - name: restart sshd
      systemd:
        name: sshd
        state: restarted
